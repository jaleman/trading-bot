# Shared Packages

This directory is reserved for shared code extracted from apps only after real reuse exists.

## Rule

Do not move the current trading bot helpers here until at least one additional app needs the same functionality.
